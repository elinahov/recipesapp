import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    row: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    star: {
        width: 12,
        height: 12,
        margin: 2,
    }
});

export default styles;
